on: [push, workflow_dispatch]

jobs:
  compile-pdf:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3 
      - uses: actions/compile-pdf@v1
      - uses: actions/upload-artifact@v3
        with:
          name: cv-pdf
          path: output/goryunov_cv.pdf